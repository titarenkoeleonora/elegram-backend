const inviteController = require('./inviteController');
const acceptController = require('./acceptController');
const rejectController = require('./rejectController');

exports.controllers = {
  inviteController,
  acceptController,
  rejectController,
};
